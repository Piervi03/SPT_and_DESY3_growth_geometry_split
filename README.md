# SPT_NC_with_DES_WL

## Installation

You need to run `python setup.py build_ext --inplace` to compile and install a cython extension.

## Data

You will most likely want access to https://github.com/SebastianBocquet/SPT_cluster_data. Just ask me!

## Example

Take a look at `setup_example.ini`.
